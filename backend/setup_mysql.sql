CREATE DATABASE IF NOT EXISTS `flumenx_portal`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- Replace the sample password before use:
-- CREATE USER 'flumenx_app'@'localhost' IDENTIFIED BY 'strong-password';
-- GRANT ALL PRIVILEGES ON `flumenx_portal`.* TO 'flumenx_app'@'127.0.0.1';
-- FLUSH PRIVILEGES;
