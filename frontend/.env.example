# FLUMENX OS Frontend Environment Variables Template

# Production API Endpoint (When running behind Nginx reverse proxy, use /api)
NEXT_PUBLIC_API_URL=/api

# For Local Development uncomment the following:
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api

# Internal Node Server Port (PM2 uses 3050)
PORT=3050
